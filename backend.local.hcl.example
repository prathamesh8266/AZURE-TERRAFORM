# Copy to backend.local.hcl and set the existing Terraform state account.
# The deployment script supplies a different key for each Infrastructure root.
resource_group_name  = "rg-terraform"
storage_account_name = "yourstateaccount123"
container_name       = "tfstate"

use_azuread_auth = true
use_cli          = true
